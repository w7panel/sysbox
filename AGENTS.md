# Agent Rules

## 通用原则

- 默认使用中文回复用户。
- 开发前先理解现有结构、边界和长期维护影响，再修改代码或配置。
- 开发涉及 containerd、snapshotter、overlay、rootfs、idmapped mount 或相关集成逻辑时，必须参考学习 `study-sourcecode/` 下的上游项目代码与实现模式，再结合本项目边界设计方案；`study-sourcecode/` 中的代码仅作为参考学习资料，不得修改。
- 优先遵循项目已有风格和约定，避免无关重构。
- 代码保持简洁、清晰、可维护；仅在有明确复用价值或边界抽象价值时提取公共逻辑，避免过度抽象。
- 修复必须面向根因，不做临时绕过、兼容旧缺陷的过渡方案或只缓解表象的补丁；若发现方案只能临时缓解，应停止实施并重新分析根因。
- 开发、测试和 LSP 诊断过程中，如环境缺少必要工具、语言服务器或依赖，可以自行安装后继续，无需单独询问授权。
- 开发和测试过程中优先使用智能体进行探索、实现、验证和复核，避免主会话积累过多无用上下文影响问题定位与交付质量。

## Git 与网络

- 本环境的全局 Git 配置文件位于 `/home/.gitconfig`；遇到 push、auth、remote rewrite 等 Git 配置问题时，先检查该文件，不要假设使用默认 `$HOME/.gitconfig`。
- `/home/.gitconfig` 中也可能包含网络代理配置；遇到 `go get`、`git clone`、`curl`、包管理器下载等访问超时问题时，先检查并优先复用这里的代理设置，不要盲目重试裸连。

## 测试集群访问

- 测试集群 kubeconfig 位于 `.omo/run-continuation/sysbox-cluster-kubeconfig.yaml`；所有集群命令必须显式添加 `--kubeconfig /home/wwwroot/sysbox/.omo/run-continuation/sysbox-cluster-kubeconfig.yaml`，不要依赖默认 kubeconfig。
- 常用调试入口是 `default/sysbox-debug-tools`。需要宿主视角时通过 `nsenter -t 1 -m -u -i -n -p -- <cmd>` 执行；宿主根目录也可从调试容器的 `/host` 访问。
- 集群验收必须围绕本次改动的实际风险面和用户可见行为设计。先明确影响的组件、生命周期阶段、内核接口、数据路径和失败模式，再选择对应的 pod 场景、压力方式、日志检查和指标；不要机械执行固定测试清单。

## 构建、安装与 Pod 重建

- 集群测试前必须编译 sysbox 全部服务并全量安装替换，避免只替换单个组件导致版本不一致或掩盖集成问题。至少包括 `sysbox-fs`、`sysbox-mgr`、`sysbox-runc`；若 `sysbox-snapshotter` 或其他组件与本次任务相关，也必须一并构建和替换。
- 将构建产物复制到 `default/sysbox-debug-tools:/tmp/`，在调试容器中先备份宿主现有二进制，例如 `cp -a /host/usr/bin/sysbox-fs /host/usr/bin/sysbox-fs.bak-$(date -u +%Y%m%d%H%M%S)`，再用 `install -m 0755` 安装到 `/host/usr/bin/`。
- 替换服务后用 `nsenter ... systemctl restart ...` 重启相关服务，并确认服务 `active`。若某组件没有新构建产物或不适用，必须在验收记录中说明原因，不要静默跳过。
- 每次集群测试涉及重启或替换 `sysbox-fs`、`sysbox-mgr`、`sysbox-runc` 时，必须在验收前销毁并重建所有使用 `runtimeClassName: sysbox-runc` 或 sysbox-runc runtime 的 pod；不要只新建临时测试 pod 而保留旧 sysbox pod。
- 重建前先列出受影响 workload 与 pod，优先通过控制器（Deployment、StatefulSet、Job 等）滚动重建；无法由控制器恢复的裸 pod 需记录并按原 spec 重建。
- 重启 `sysbox-fs` 会丢失既有 sysbox 容器注册状态；旧 pod 可能出现 `Received seccompNotifMsg generated by unknown container` 或 `unsafe procfs detected`，不要用重启前已存在的 sysbox pod 做功能验收。应在全部 sysbox-runc pod 完成销毁重建后，再用全新临时 `runtimeClassName: sysbox-runc`、`hostUsers: false` pod 验收。

## sysbox-snapshotter 状态与故障排查

- 排查 `sysbox-snapshotter` 时，先确认宿主 `/etc/systemd/system/sysbox-snapshotter.service` 的 `ExecStart` 参数与 `/host/usr/bin/sysbox-snapshotter` 实际支持的 flags 匹配，尤其是当前支持的 `--address`、`--root`。遇到 `flag provided but not defined` 时，先检查服务是否仍传入旧参数（例如历史版本的 `--socket-path`、`--state-root`、`--overlay-root`），再检查 `/host/usr/bin/sysbox-snapshotter.bak-*` 与服务日志，不要假设当前二进制一定来自本工作区构建。
- 清理 `sysbox-snapshotter` 或 containerd snapshot 状态前，必须先停止会自动重建 sysbox pod 的 workload（Deployment、StatefulSet、Job 等）或停止 k3s，避免控制器在清理过程中重新写入半新半旧状态。
- 不要只删除 `/var/lib/rancher/k3s/agent/containerd/io.containerd.snapshotter.v1.sysbox` 或 `/run/sysbox-snapshotter` 而不冻结 workload；这样容易把问题从 `unsafe rootfs metadata key` 转成 `target snapshot ... already exists` 等状态不一致错误。
- rootfs rw-layer 配置来自 admission 注入的 sidecar OCI spec 与 `ROOTFS_RW_LAYER_SPEC`，不再读取文件型 metadata registry。优先按 snapshotter 请求身份、sidecar spec、snapshotter state root 一致性排查，不要误判为 `sysbox-runc` rootfs shifting 逻辑问题。
- 交付 rootfs rw-layer 相关改动时，验收必须证明三件事：snapshotter 解析到正确业务容器身份、sidecar intent 与 PVC mount source 可见、overlay mount 的 `upperdir/workdir` 实际改写到 PVC-backed 路径；不要只看 Pod Running 或容器内文件存在。
- 不要把 Pod annotation、Pod label、业务容器 env 当作 snapshotter 必然可见的数据面。若改动依赖这些字段传递到 snapshotter，必须用 `ctr`/日志/测试证明它们确实进入业务容器 snapshot `Prepare` labels 或 containerd metadata。
- 如果改动增加 containerd socket 查询、sidecar spec 读取或 Pod 创建热路径逻辑，验收需包含并发创建或批量创建场景，并检查 containerd 与 `sysbox-snapshotter` 日志中没有 metadata RPC 超时、明显排队或重复扫描导致的性能问题。
- `sysbox-snapshotter` 状态清理后必须按顺序重启相关服务（必要时 k3s、`sysbox-snapshotter`、`sysbox-fs`、`sysbox-mgr`），再销毁并重建所有 sysbox-runc pod。验收前用全新临时 sysbox pod 验证 baseline 创建路径，确认不再出现 `unsafe rootfs metadata key`、`target snapshot ... already exists`、snapshot unpack/prepare 失败后，再验证本次代码改动。

## containerd / sysbox-snapshotter 开发契约

- 修改 `sysbox-snapshotter`、containerd proxy plugin 配置、snapshotter remap 或 idmapped mount 行为前，必须先读清 containerd 插件机制，不要凭直觉把 native plugin 与 proxy plugin 混为一谈。
- native snapshotter 可在 plugin init 中写 `ic.Meta.Capabilities`；外部 proxy snapshotter 的 capabilities 由 containerd 配置 `proxy_plugins.<name>.capabilities` 静态注入，不会从远端 snapshotter gRPC 服务动态发现。
- `sysbox-snapshotter` 是否启用 idmapped mount 必须由 containerd proxy plugin 的 `remap-ids` capability 决定。containerd 的 `sysbox` proxy plugin 显示 `Capabilities: remap-ids` 时，snapshotter 才能启用 `overlay.WithRemapIDs` 并返回 `uidmap=` / `gidmap=`；未显示 `remap-ids` 时必须降级为非 idmapped 视图，禁止返回 `uidmap=` / `gidmap=`。
- 禁止出现“containerd 先创建 fallback `*-remap` parent，snapshotter 随后又返回 idmapped active mount”的混合 remap rootfs。这类状态通常会在容器 init 阶段表现为 `open etc/hosts: permission denied`、`/dev/kmsg: no such file or directory` 等 runc rootfs setup 错误。
- idmapped 模式只需要声明 `capabilities = ["remap-ids"]`。不要默认声明 `only-remap-ids`；未设置 `remap-ids` 时允许降级，让 containerd 按自身逻辑走 fallback remap/chown 路径。
- k3s 会从 `/var/lib/rancher/k3s/agent/etc/containerd/config-v3.toml.tmpl` 重新生成 `/var/lib/rancher/k3s/agent/etc/containerd/config.toml`；需要持久修改 containerd proxy plugin 配置时，优先修改模板而不是只改生成后的 `config.toml`。修改后必须重启 k3s，并用 `ctr plugins ls -d id==sysbox` 验证 `Capabilities` 已生效。
- `sysbox-snapshotter` 只应用于 `sysbox-runc` runtime path，不要把全局 CRI image snapshotter 改成 `sysbox`，除非任务明确要求并完成完整镜像拉取、unpack、GC、snapshot lifecycle 验证。常规配置应保持全局 image snapshotter 为 `overlayfs`，仅在 `plugins.'io.containerd.cri.v1.runtime'.containerd.runtimes.sysbox-runc.snapshotter = "sysbox"` 上接入。
- 如果希望通过 snapshot labels 直接把 rootfs rw-layer 数据传给 snapshotter，必须先确认 containerd CRI 实际会在业务容器 snapshot `Prepare` 请求中传递这些 labels。只把字段写进 Pod annotation、Pod label 或业务容器 env，不会自动出现在 snapshotter API 的 `opts` / `snapshots.Info.Labels` 中。
- 看到 `open etc/hosts: permission denied`、`/dev/kmsg: no such file or directory` 等 runc rootfs setup 错误时，不要立即 patch `sysbox-runc`。先检查 snapshotter/rootfs 权限视图、containerd remap labels、proxy capabilities、active/parent snapshot ownership 和 mount options；这些 runc 错误经常只是 snapshotter/containerd remap 契约错误的下游表现。

## 集群验收建议

- passthrough 相关改动可在新建 sysbox pod 内执行循环检查：读取 `cat /proc/sys/kernel/hostname`、`ls /proc/sys/net/ipv4`、`ls /sys/kernel`。
- proc 资源视图相关改动可执行循环检查：`cat /proc/meminfo`、`cat /proc/stat`、`cat /proc/loadavg`、`cat /sys/devices/system/cpu/online`、`free`。
- rootfs rw-layer 相关改动必须用全新 `runtimeClassName: sysbox-runc`、`hostUsers: false` Pod 验证：容器内写入/删除 rootfs 文件后删除 Pod，再用相同 PVC/path 重建并确认数据和 whiteout 仍生效；同时用宿主侧 `ctr -n k8s.io snapshots --snapshotter sysbox mounts <container-id>` 或等价证据确认 `upperdir/workdir` 指向 PVC-backed 目录。
- rootfs rw-layer 失败路径也要验收：未配置 intent 的容器应返回原生 mounts；已解析到当前容器 intent 后，PVC mount 缺失、非法 `path`、非空外部目录或 preparer 失败必须阻止容器启动并给出可定位日志。
- sysbox-snapshotter 相关交付至少覆盖四类全新 Pod 场景：containerd `sysbox` proxy plugin 配置 `capabilities = ["remap-ids"]` + 有 rootfs rw-layer annotation；配置 `remap-ids` + 无 rootfs rw-layer annotation；不配置 `remap-ids` + 有 rootfs rw-layer annotation；不配置 `remap-ids` + 无 rootfs rw-layer annotation。四类场景都必须记录 Pod 创建结果、snapshot mounts、snapshot 列表和关键服务日志。
- 涉及 idmapped mount / userns remap 的修复必须同时验证三层证据：`ctr plugins ls -d id==sysbox` 显示期望 capabilities；`ctr -n k8s.io snapshots --snapshotter sysbox mounts <container-id>` 的 mount options 与模式匹配；`ctr -n k8s.io snapshots --snapshotter sysbox ls | grep -- -remap` 与当前 remap 模式一致，不出现与本次路径相冲突的新 fallback remap snapshot。
- 验收后检查日志：`journalctl -u sysbox-fs -u sysbox-mgr -u sysbox-snapshotter --since '5 minutes ago' --no-pager`，重点确认无 `panic`、`fatal`、`OOM`、`ENOMEM`、`unsafe procfs`、`thread-self`、`Unexpected error during NotifReceive`、`openat2 seccomp stats`、`unsafe rootfs metadata key`、snapshot prepare/unpack 错误。
- 临时测试 pod 验收完成后必须删除。
