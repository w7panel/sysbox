# Agent Rules

- 默认使用中文回复用户。
- 开发时保持架构思维，先理解现有结构、边界和长期维护影响，再进行修改。
- 代码应保持简洁、清晰、可维护，避免不必要的复杂度。
- 仅在存在明确复用价值或边界抽象价值时提取公共逻辑，避免过度抽象。
- 优先遵循项目已有风格和约定，避免无关重构。
- 开发方案必须面向根因彻底解决问题，不做临时绕过、兼容旧缺陷的过渡方案或只缓解表象的补丁；如发现当前方案只能临时缓解，应停止实施并重新分析根因。

## 测试集群访问与验收

- 测试集群 kubeconfig 位于 `.omo/run-continuation/sysbox-cluster-kubeconfig.yaml`，所有集群命令显式添加 `--kubeconfig /home/wwwroot/sysbox/.omo/run-continuation/sysbox-cluster-kubeconfig.yaml`，不要依赖默认 kubeconfig。
- 常用调试入口是 `default/sysbox-debug-tools`。需要宿主视角时通过 `nsenter -t 1 -m -u -i -n -p -- <cmd>` 执行；宿主根目录也可从调试容器的 `/host` 访问。
- 部署新 `sysbox-fs` 二进制时，先 `kubectl cp sysbox-fs/build/sysbox-fs default/sysbox-debug-tools:/tmp/sysbox-fs-new`，再在调试容器中备份并安装到宿主：`cp -a /host/usr/bin/sysbox-fs /host/usr/bin/sysbox-fs.bak-$(date -u +%Y%m%d%H%M%S)`，`install -m 0755 /tmp/sysbox-fs-new /host/usr/bin/sysbox-fs`，最后用 `nsenter ... systemctl restart sysbox-fs` 重启。
- 每次集群测试涉及重启或替换 `sysbox-fs` / `sysbox-mgr` / `sysbox-runc` 时，必须在验收前销毁并重建所有使用 `runtimeClassName: sysbox-runc` 或 sysbox-runc runtime 的 pod；不要只新建临时测试 pod 而保留旧 sysbox pod。重建前先列出受影响 workload 与 pod，优先通过其控制器（Deployment/StatefulSet/Job 等）滚动重建，无法由控制器恢复的裸 pod 需记录并按原 spec 重建。
- 重启 `sysbox-fs` 会丢失既有 sysbox 容器的注册状态；旧 pod 可能出现 `Received seccompNotifMsg generated by unknown container` 或 `unsafe procfs detected`，不要用重启前已存在的 sysbox pod 做功能验收。应在全部 sysbox-runc pod 完成销毁重建后，再新建临时 `runtimeClassName: sysbox-runc`、`hostUsers: false` 的测试 pod，让它在当前 `sysbox-fs` 上重新注册后再验收。
- passthrough 验收建议在新建 sysbox pod 内执行：循环 200 次读取 `cat /proc/sys/kernel/hostname`、`ls /proc/sys/net/ipv4`、`ls /sys/kernel`；proc 资源视图验收执行 200 次 `cat /proc/meminfo`、`cat /proc/stat`、`cat /proc/loadavg`、`cat /sys/devices/system/cpu/online`、`free`。
- 验收后检查日志：`journalctl -u sysbox-fs -u sysbox-mgr --since '5 minutes ago' --no-pager`，重点确认无 `panic`、`fatal`、`OOM`、`ENOMEM`、`unsafe procfs`、`thread-self`、`Unexpected error during NotifReceive`、`openat2 seccomp stats`。临时测试 pod 验收完成后必须删除。
